[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "tech-manager"
version = "0.1.0"
description = "Tech management laboratory project"
requires-python = ">=3.11"

[project.scripts]
Tech-manager = "tech_manager.main:main"
